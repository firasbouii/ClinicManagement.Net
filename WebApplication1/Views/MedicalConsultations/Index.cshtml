@model IEnumerable<WebApplication1.Models.MedicalConsultation>

@{
    ViewBag.Title = "Index";
}

<h2 style="font-style: italic; color:#000080;   font-family: 'Comfortaa', cursive;">Medical Consultations</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table" style=" font-family: 'Comfortaa', cursive;">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Doctor.DrName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Patient.FullName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Prescription.note)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RoomNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateOfmedicalconsultation)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Doctor.DrName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Patient.FullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Prescription.note)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RoomNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateOfmedicalconsultation)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.id }) |
                @Html.ActionLink("Details", "Details", new { id = item.id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.id })
            </td>
        </tr>
    }

</table>
